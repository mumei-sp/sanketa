/**
 * Shared constants for attendance feature
 * Centralizes error messages, labels, and other repeated strings
 */

export const ATTENDANCE_MESSAGES = {
  LOADING: 'Loading attendance...',
  NOT_FOUND: 'Attendance data not found',
  BACK_TO_ATTENDANCE: 'Back to Attendance',
} as const

export const ATTENDANCE_LABELS = {
  ATTENDANCE: 'Attendance',
} as const

