/** Barrel for `time-table` — rooms, bell times, the grid and the calendar. */

export {
  listTeachingSlots,
  listTimeSlots,
  listRooms,
  findRoom,
  roomLabel,
  listCategories,
  listTimetable,
  listEvents,
  attendeesOf,
  replaceTimetable,
  replaceEvents,
} from './store'
export type * from './types'
